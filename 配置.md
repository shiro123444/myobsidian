
1. 安装 Node.js
    
    前往 [Node.js 官网](https://nodejs.org/) 下载 Windows LTS 安装包完成安装，并勾选 "Automatically install necessary tools"。
    
    也可以在管理员 PowerShell 中运行：
    
    ```powershell
    winget install -e --id OpenJS.NodeJS.LTS
    ```

    
2. 验证 npm 是否就绪
    
    在 PowerShell 中逐个执行下方命令验证 PATH：

    ```powershell
    node -v
    npm -v
    ```

    如命令不可用，请重新打开终端或将 Node.js 安装目录加入系统环境变量。
    
3. 安装 Codex CLI
    
    以管理员 PowerShell 执行：
    
    ```bash
    npm i -g @openai/codex
    ```

    安装完成后重新打开终端，确保命令可用。
    
4. 准备 Codex 配置文件
    
    配置文件位于相应目录，同目录还需要 auth.json 存放 Access Token，可用 PowerShell 快速创建：

    ```bash
    mkdir %USERPROFILE%\.codex
    notepad %USERPROFILE%\.codex\config.toml
    notepad %USERPROFILE%\.codex\auth.json
    ```

    
    先将下方 TOML 配置粘贴进 config.toml，再把 Access Token 写入 auth.json。
    
    ```bash
    model = "gpt-5.3-codex"
    model_provider = "mirror"
    
    [model_providers.mirror]
    name = "mirror"
    base_url = "https://chat.soruxgpt.com/codex"
    wire_api = "responses"
    requires_openai_auth = true
    ```

    
    当前 Base URL：https://chat.soruxgpt.com/codex
    
    同时在 auth.json 写入以下内容，并将 Access Token 替换为上方复制的值：
    
    ```bash
    {
      "OPENAI_API_KEY": "eyJhbGciOiJub25lIiwidHlwIjoiSldUIn0.eyJodHRwczovL2FwaS5vcGVuYWkuY29tL2F1dGgiOnsiY2hhdGdwdF9hY2NvdW50X2lkIjoiZGVmYXVsdCJ9LCJzdWIiOiJ1c2VyLW1pcnJvciIsImFjY2Vzc190b2tlbiI6IjEzN2VlZGJmLTZiZDYtNDI5Yi1iMWY4LTRiNTIwYzRmZGUyYyJ9."
    }
    ```

    
5. 启动并登录
    
    在任意终端执行 codex，选择 API Key 登录方式，输入上方 Access Token 即可。